# TODO

# Docker needed in the raspberry to run the file, sudo usermod -a -G docker $USER, needed to do this only once follow this stack: https://stackoverflow.com/questions/47854463/docker-got-permission-denied-while-trying-to-connect-to-the-docker-daemon-socke 
# Our container name is scraper on our raspberry we rebuild everytime because why not
# Our logs and databases are saved according to the information dumped in the docker
# The commands in "production" to scrap the data is found in cronjob.txt